# ===========================================
# TEMPO API CONFIGURATION
# ===========================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git!

# Server Configuration
PORT=8080
ENVIRONMENT=development

# Database Configuration
# Format: postgres://user:password@host:port/database?sslmode=disable
# For local development with Docker:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tempo?sslmode=disable

# For Neon (serverless PostgreSQL):
# DATABASE_URL=postgres://user:password@ep-xxx.us-east-2.aws.neon.tech/tempo?sslmode=require

# Connection pool settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# JWT Configuration
# Generate a secure secret with: openssl rand -hex 32
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h

# Redis Configuration (optional, for rate limiting and caching)
REDIS_URL=redis://localhost:6379

